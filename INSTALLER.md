# Installer moved

The Windows installer now lives **outside** all service repos:

`..\..\installer\`  
(full path example: `C:\Users\User\Desktop\New folder (7)\installer`)

Build:

```powershell
cd "..\..\installer"
.\build-setup.ps1
```
