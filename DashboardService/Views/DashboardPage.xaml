<Page x:Class="DashboardService.Views.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      FontFamily="Bahnschrift, Segoe UI"
      Background="{DynamicResource AppBgBrush}">

    <Page.Resources>

        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <!-- ================================ -->
        <!-- COLORS / STYLES -->
        <!-- ================================ -->

        <Style x:Key="SidebarButtonStyle"
               TargetType="Button">

            <Setter Property="Height" Value="48"/>
            <Setter Property="Margin" Value="0,3,0,3"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource SidebarTextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>

            <Setter Property="Template">

                <Setter.Value>

                    <ControlTemplate TargetType="Button">

                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="12"
                                BorderThickness="0">

                            <ContentPresenter
                                VerticalAlignment="Center"
                                HorizontalAlignment="Stretch"
                                Margin="16,0"/>

                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsMouseOver"
                                     Value="True">

                                <Setter TargetName="ButtonBorder"
                                        Property="Background"
                                        Value="{DynamicResource SidebarHoverBgBrush}"/>

                                <Setter Property="Foreground"
                                        Value="{DynamicResource SidebarTextActiveBrush}"/>

                            </Trigger>

                        </ControlTemplate.Triggers>

                    </ControlTemplate>

                </Setter.Value>

            </Setter>

        </Style>

        <Style x:Key="KpiCardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="20,18"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource CardBorderBrush}"/>
            <Setter Property="Background" Value="{DynamicResource CardBgBrush}"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.12" Color="#000000"/>
                </Setter.Value>
            </Setter>
        </Style>


        <!-- ================================ -->
        <!-- DATAGRID STYLE -->
        <!-- ================================ -->

        <Style TargetType="DataGrid">

            <Setter Property="Background"
                    Value="White"/>

            <Setter Property="BorderThickness"
                    Value="0"/>

            <Setter Property="RowBackground"
                    Value="White"/>

            <Setter Property="AlternatingRowBackground"
                    Value="#F8FAFC"/>

            <Setter Property="HorizontalGridLinesBrush"
                    Value="#E9EEF5"/>

            <Setter Property="VerticalGridLinesBrush"
                    Value="Transparent"/>

            <Setter Property="GridLinesVisibility"
                    Value="Horizontal"/>

            <Setter Property="HeadersVisibility"
                    Value="Column"/>

            <Setter Property="RowHeight"
                    Value="58"/>

            <Setter Property="CanUserResizeRows"
                    Value="False"/>

            <Setter Property="CanUserSortColumns"
                    Value="False"/>

            <Setter Property="FontSize"
                    Value="14"/>

        </Style>


        <!-- DATAGRID HEADER -->

        <Style TargetType="DataGridColumnHeader">

            <Setter Property="Background"
                    Value="#F7F9FC"/>

            <Setter Property="Foreground"
                    Value="#64748B"/>

            <Setter Property="FontWeight"
                    Value="SemiBold"/>

            <Setter Property="FontSize"
                    Value="12"/>

            <Setter Property="Height"
                    Value="46"/>

            <Setter Property="Padding"
                    Value="16,0"/>

            <Setter Property="BorderBrush"
                    Value="#E7EDF4"/>

            <Setter Property="BorderThickness"
                    Value="0,0,0,1"/>

        </Style>


        <!-- DATAGRID CELL -->

        <Style TargetType="DataGridCell">

            <Setter Property="BorderThickness"
                    Value="0"/>

            <Setter Property="Padding"
                    Value="16,0"/>

            <Setter Property="VerticalContentAlignment"
                    Value="Center"/>

            <Setter Property="Foreground"
                    Value="#334155"/>

        </Style>


        <!-- DATAGRID ROW -->

        <Style TargetType="DataGridRow">

            <Setter Property="Background"
                    Value="White"/>

            <Setter Property="BorderBrush"
                    Value="#E9EEF5"/>

            <Setter Property="BorderThickness"
                    Value="0,0,0,1"/>

            <Setter Property="Template">

                <Setter.Value>

                    <ControlTemplate TargetType="DataGridRow">

                        <Border x:Name="RowBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">

                            <SelectiveScrollingGrid>

                                <SelectiveScrollingGrid.ColumnDefinitions>

                                    <ColumnDefinition Width="Auto"/>

                                    <ColumnDefinition Width="*"/>

                                </SelectiveScrollingGrid.ColumnDefinitions>

                                <DataGridCellsPresenter
                                    Grid.Column="1"/>

                            </SelectiveScrollingGrid>

                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsMouseOver"
                                     Value="True">

                                <Setter TargetName="RowBorder"
                                        Property="Background"
                                        Value="#F5F9FF"/>

                            </Trigger>

                            <Trigger Property="AlternationIndex"
                                     Value="1">

                                <Setter TargetName="RowBorder"
                                        Property="Background"
                                        Value="#FAFBFD"/>

                            </Trigger>

                        </ControlTemplate.Triggers>

                    </ControlTemplate>

                </Setter.Value>

            </Setter>

        </Style>


        <!-- ================================ -->
        <!-- PROGRESS BAR -->
        <!-- ================================ -->

        <Style TargetType="ProgressBar">

            <Setter Property="Height"
                    Value="8"/>

            <Setter Property="Background"
                    Value="#E7EDF4"/>

            <Setter Property="BorderThickness"
                    Value="0"/>

            <Setter Property="Template">

                <Setter.Value>

                    <ControlTemplate TargetType="ProgressBar">

                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4">

                            <Grid ClipToBounds="True">

                                <Border x:Name="PART_Track"
                                        CornerRadius="4"/>

                                <Border x:Name="PART_Indicator"
                                        Background="#0A3776"
                                        HorizontalAlignment="Left"
                                        CornerRadius="4"/>

                            </Grid>

                        </Border>

                    </ControlTemplate>

                </Setter.Value>

            </Setter>

        </Style>

    </Page.Resources>


    <!-- ===================================== -->
    <!-- MAIN LAYOUT -->
    <!-- ===================================== -->

    <Grid>

        <Grid.ColumnDefinitions>

            <ColumnDefinition Width="230"/>

            <ColumnDefinition Width="*"/>

        </Grid.ColumnDefinitions>


        <!-- ===================================== -->
        <!-- SIDEBAR -->
        <!-- ===================================== -->

        <Border Grid.Column="0" ClipToBounds="True"
                Background="{DynamicResource SidebarBgBrush}">

            <Grid>

                <Ellipse Width="220" Height="220"
                         Fill="#14FFFFFF"
                         HorizontalAlignment="Left"
                         VerticalAlignment="Top"
                         Margin="-90,-80,0,0"
                         IsHitTestVisible="False"/>

                <Ellipse Width="160" Height="160"
                         Fill="#10FFFFFF"
                         HorizontalAlignment="Right"
                         VerticalAlignment="Bottom"
                         Margin="0,0,-50,-40"
                         IsHitTestVisible="False"/>

                <!-- Sci-fi grid lines (visible in dark mode via overlay opacity) -->
                <Border x:Name="SidebarSciFiOverlay"
                        Opacity="0"
                        IsHitTestVisible="False">
                    <Border.Background>
                        <DrawingBrush TileMode="Tile" Viewport="0,0,28,28" ViewportUnits="Absolute">
                            <DrawingBrush.Drawing>
                                <GeometryDrawing>
                                    <GeometryDrawing.Pen>
                                        <Pen Brush="#3322D3EE" Thickness="0.6"/>
                                    </GeometryDrawing.Pen>
                                    <GeometryDrawing.Geometry>
                                        <GeometryGroup>
                                            <LineGeometry StartPoint="0,0" EndPoint="28,0"/>
                                            <LineGeometry StartPoint="0,0" EndPoint="0,28"/>
                                        </GeometryGroup>
                                    </GeometryDrawing.Geometry>
                                </GeometryDrawing>
                            </DrawingBrush.Drawing>
                        </DrawingBrush>
                    </Border.Background>
                </Border>

                <Grid>

                <Grid.RowDefinitions>

                    <RowDefinition Height="160"/>

                    <RowDefinition Height="*"/>

                    <RowDefinition Height="95"/>

                </Grid.RowDefinitions>


                <!-- BRAND -->

                <StackPanel Grid.Row="0"
                            Margin="24,32,20,0">

                    <Border Width="48" Height="48"
                            CornerRadius="14"
                            Background="#22FFFFFF"
                            HorizontalAlignment="Left"
                            Margin="0,0,0,16"
                            Padding="6">
                        <Image Source="pack://application:,,,/Assets/logo.png"
                               Stretch="Uniform"
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                    </Border>

                    <TextBlock Text="SRP INNOVATIONS"
                               FontSize="11"
                               FontWeight="Bold"
                               Foreground="#9FC8F5"/>

                    <TextBlock Text="Smart Chamber"
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="White"
                               Margin="0,6,0,0"/>

                    <TextBlock Text="Monitoring"
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="White"
                               Margin="0,-2,0,0"/>

                    <Border Height="1"
                            Background="#33FFFFFF"
                            Margin="0,18,20,0"/>

                </StackPanel>


                <!-- MENU -->

                <StackPanel Grid.Row="1"
                            Margin="14,10,14,0">


                    <!-- DASHBOARD -->

                    <Button Style="{StaticResource SidebarButtonStyle}"
                            Background="{DynamicResource SidebarActiveBgBrush}"
                            Foreground="{DynamicResource SidebarTextActiveBrush}"
                            Click="DashboardMenu_Click">

                        <StackPanel Orientation="Horizontal">

                            <TextBlock Text="▦"
                                       FontSize="19"
                                       Width="30"
                                       VerticalAlignment="Center"/>

                            <TextBlock Text="Dashboard"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Center"/>

                        </StackPanel>

                    </Button>


                    <!-- CHAMBERS -->

                    <Button Style="{StaticResource SidebarButtonStyle}"
                            Click="ChambersMenu_Click">

                        <StackPanel Orientation="Horizontal">

                            <TextBlock Text="◈"
                                       FontSize="18"
                                       Width="30"
                                       VerticalAlignment="Center"/>

                            <TextBlock Text="Chambers"
                                       VerticalAlignment="Center"/>

                        </StackPanel>

                    </Button>


                    <!-- EMPLOYEES -->

                    <Button Style="{StaticResource SidebarButtonStyle}"
                            Click="EmployeesMenu_Click">

                        <StackPanel Orientation="Horizontal">

                            <TextBlock Text="♙"
                                       FontSize="18"
                                       Width="30"
                                       VerticalAlignment="Center"/>

                            <TextBlock Text="Employees"
                                       VerticalAlignment="Center"/>

                        </StackPanel>

                    </Button>

                    <Button Style="{StaticResource SidebarButtonStyle}"
                            Click="ReadersMenu_Click">

                        <StackPanel Orientation="Horizontal">

                            <TextBlock Text="⊛"
                                       FontSize="18"
                                       Width="30"
                                       VerticalAlignment="Center"/>

                            <TextBlock Text="RFID Readers"
                                       VerticalAlignment="Center"/>

                        </StackPanel>

                    </Button>

                    <Button Style="{StaticResource SidebarButtonStyle}"
                            Click="ReportsToggle_Click">

                        <Grid>
                            <StackPanel Orientation="Horizontal">

                                <TextBlock Text="▤"
                                           FontSize="18"
                                           Width="30"
                                           VerticalAlignment="Center"/>

                                <TextBlock Text="Reports"
                                           VerticalAlignment="Center"/>

                            </StackPanel>

                            <TextBlock x:Name="ReportsArrowText"
                                       Text="▼"
                                       FontSize="10"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center"
                                       Margin="0,0,4,0"/>
                        </Grid>

                    </Button>

                    <StackPanel x:Name="ReportsSubMenuPanel"
                                Margin="18,0,0,4"
                                Visibility="Collapsed">

                        <Button Style="{StaticResource SidebarButtonStyle}"
                                Height="40"
                                Click="EntryExitReportMenu_Click">

                            <StackPanel Orientation="Horizontal">

                                <TextBlock Text="▸"
                                           FontSize="14"
                                           Width="24"
                                           VerticalAlignment="Center"/>

                                <TextBlock Text="Entry / Exit"
                                           VerticalAlignment="Center"/>

                            </StackPanel>

                        </Button>

                        <Button Style="{StaticResource SidebarButtonStyle}"
                                Height="40"
                                Click="ChamberEmployeesReportMenu_Click">

                            <StackPanel Orientation="Horizontal">

                                <TextBlock Text="▸"
                                           FontSize="14"
                                           Width="24"
                                           VerticalAlignment="Center"/>

                                <TextBlock Text="Chamber Employees"
                                           VerticalAlignment="Center"/>

                            </StackPanel>

                        </Button>

                    </StackPanel>

                </StackPanel>


                <!-- FOOTER -->

                <StackPanel Grid.Row="2"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            Margin="12,0,12,10">

                    <TextBlock Text="© 2026 SRP Innovations"
                               Foreground="#9FC8F5"
                               FontSize="10"
                               TextAlignment="Center"
                               HorizontalAlignment="Center"/>

                    <TextBlock Margin="0,8,0,0"
                               FontSize="10"
                               TextAlignment="Center"
                               HorizontalAlignment="Center"
                               Foreground="#A8C8EC">
                        <Run Text="Designed &amp; Developed by "/>
                        <Hyperlink NavigateUri="https://codeinq.com/"
                                   RequestNavigate="CodeInqLink_RequestNavigate"
                                   Foreground="#FFFFFF"
                                   FontWeight="SemiBold"
                                   TextDecorations="Underline"
                                   Cursor="Hand">
                            <Run Text="CodeInQ"/>
                        </Hyperlink>
                    </TextBlock>

                </StackPanel>

            </Grid>

            </Grid>

        </Border>


        <!-- ===================================== -->
        <!-- CONTENT -->
        <!-- ===================================== -->

        <Grid Grid.Column="1"
              Margin="28,22,28,22"
              Background="{DynamicResource ContentAreaBgBrush}">

            <!-- Sci-fi atmosphere overlay -->
            <Border x:Name="ContentSciFiOverlay"
                    Opacity="0"
                    IsHitTestVisible="False"
                    Panel.ZIndex="0">
                <Border.Background>
                    <DrawingBrush TileMode="Tile" Viewport="0,0,48,48" ViewportUnits="Absolute">
                        <DrawingBrush.Drawing>
                            <GeometryDrawing>
                                <GeometryDrawing.Pen>
                                    <Pen Brush="#1422D3EE" Thickness="0.5"/>
                                </GeometryDrawing.Pen>
                                <GeometryDrawing.Geometry>
                                    <GeometryGroup>
                                        <LineGeometry StartPoint="0,0" EndPoint="48,0"/>
                                        <LineGeometry StartPoint="0,0" EndPoint="0,48"/>
                                    </GeometryGroup>
                                </GeometryDrawing.Geometry>
                            </GeometryDrawing>
                        </DrawingBrush.Drawing>
                    </DrawingBrush>
                </Border.Background>
            </Border>

            <Grid Panel.ZIndex="1">

            <Grid.RowDefinitions>

                <RowDefinition Height="Auto"/>

                <RowDefinition Height="118"/>

                <RowDefinition Height="210"/>

                <RowDefinition Height="*"/>

            </Grid.RowDefinitions>


            <!-- ===================================== -->
            <!-- HEADER -->
            <!-- ===================================== -->

            <Grid Margin="0,0,0,14" Panel.ZIndex="1000">

                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Left" Margin="0,0,320,0">
                    <TextBlock Text="OPERATIONS"
                               FontSize="11"
                               FontWeight="Bold"
                               Foreground="{DynamicResource TextSecondaryBrush}"/>
                    <TextBlock x:Name="DashboardTitleText"
                               Text="Live Dashboard"
                               FontSize="28"
                               FontWeight="Bold"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               Margin="0,2,0,0"/>
                    <TextBlock Text="Chamber occupancy, alerts and member timers"
                               FontSize="12"
                               Foreground="{DynamicResource TextMutedBrush}"
                               Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top">

                    <!-- DARK MODE TOGGLE -->
                    <Border x:Name="ThemeToggleButton"
                            Background="{DynamicResource ProfileBgBrush}"
                            BorderBrush="{DynamicResource CardBorderBrush}"
                            BorderThickness="1"
                            CornerRadius="12"
                            Padding="12,8"
                            Margin="0,0,12,0"
                            Cursor="Hand"
                            VerticalAlignment="Center"
                            MouseLeftButtonUp="ThemeToggle_MouseLeftButtonUp">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="ThemeToggleTrack"
                                    Width="42"
                                    Height="22"
                                    CornerRadius="11"
                                    Background="{DynamicResource ThemeToggleTrackBrush}"
                                    VerticalAlignment="Center"
                                    Margin="0,0,10,0">
                                <Border x:Name="ThemeToggleKnob"
                                        Width="16"
                                        Height="16"
                                        CornerRadius="8"
                                        Background="{DynamicResource ThemeToggleKnobBrush}"
                                        HorizontalAlignment="Left"
                                        Margin="3,0,0,0"
                                        VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock x:Name="ThemeToggleLabel"
                                           Text="DARK MODE"
                                           FontSize="10"
                                           FontWeight="Bold"
                                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock x:Name="ThemeToggleStateText"
                                           Text="OFF"
                                           FontSize="9"
                                           Foreground="{DynamicResource TextMutedBrush}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

            <!-- Profile container: relative positioning equivalent (Grid + Popup overlay) -->
            <Grid HorizontalAlignment="Right"
                  VerticalAlignment="Top"
                  Panel.ZIndex="1000">

                <!-- ADMINISTRATOR BUTTON -->

                <Border x:Name="AdminMenuButton"
                        Background="{DynamicResource ProfileBgBrush}"
                        BorderBrush="{DynamicResource CardBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="12"
                        Padding="8"
                        Cursor="Hand"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        MouseLeftButtonUp="AdminMenuButton_MouseLeftButtonUp">

                    <Border.Effect>
                        <DropShadowEffect BlurRadius="18"
                                          ShadowDepth="2"
                                          Opacity="0.06"/>
                    </Border.Effect>

                    <StackPanel Orientation="Horizontal">

                        <Border Width="38"
                                Height="38"
                                Background="#0A3776"
                                CornerRadius="19">

                            <TextBlock x:Name="AdminInitialText"
                                       Text="A"
                                       Foreground="White"
                                       FontWeight="SemiBold"
                                       FontSize="15"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>

                        </Border>

                        <StackPanel Margin="10,0,8,0"
                                    VerticalAlignment="Center">

                            <TextBlock x:Name="AdminNameText"
                                       Text="Administrator"
                                       FontSize="13"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextPrimaryBrush}"/>

                            <TextBlock x:Name="AdminRoleText"
                                       Text="ADMIN"
                                       FontSize="9"
                                       Foreground="{DynamicResource TextMutedBrush}"
                                       Margin="0,2,0,0"/>

                        </StackPanel>

                        <TextBlock x:Name="AdminMenuArrow"
                                   Text="▼"
                                   FontSize="9"
                                   Foreground="#8793A3"
                                   VerticalAlignment="Center"
                                   Margin="0,0,4,0"/>

                    </StackPanel>

                </Border>


                <!-- OVERLAY MENU: floats above dashboard, does not affect layout flow -->

                <Popup x:Name="AdminMenuPopup"
                       PlacementTarget="{Binding ElementName=AdminMenuButton}"
                       Placement="Bottom"
                       VerticalOffset="8"
                       AllowsTransparency="True"
                       StaysOpen="True"
                       PopupAnimation="Fade"
                       Closed="AdminMenuPopup_Closed">

                    <Border x:Name="AdminMenuPanel"
                            Width="230"
                            Background="{DynamicResource ModalCardBrush}"
                            CornerRadius="14"
                            Padding="8"
                            BorderBrush="{DynamicResource CardBorderBrush}"
                            BorderThickness="1"
                            SnapsToDevicePixels="True">

                        <Border.Effect>
                            <DropShadowEffect BlurRadius="18"
                                              ShadowDepth="3"
                                              Opacity="0.14"/>
                        </Border.Effect>

                        <StackPanel>

                            <Button Click="Logout_Click"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Padding="10,10"
                                    HorizontalContentAlignment="Left"
                                    Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="ItemBorder"
                                                Background="Transparent"
                                                CornerRadius="10"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Left"
                                                              VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ItemBorder"
                                                        Property="Background"
                                                        Value="#FEF2F2"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>

                                <StackPanel Orientation="Horizontal">
                                    <Border Width="32"
                                            Height="32"
                                            Background="#FEF2F2"
                                            CornerRadius="8"
                                            Margin="0,0,12,0">
                                        <TextBlock Text="OUT"
                                                   FontSize="9"
                                                   FontWeight="Bold"
                                                   Foreground="#DC2626"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"/>
                                    </Border>

                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="Logout"
                                                   FontSize="13"
                                                   FontWeight="SemiBold"
                                                   Foreground="#DC2626"/>
                                        <TextBlock Text="Sign out from dashboard"
                                                   FontSize="11"
                                                   Foreground="#94A3B8"
                                                   Margin="0,2,0,0"/>
                                    </StackPanel>
                                </StackPanel>
                            </Button>

                        </StackPanel>

                    </Border>

                </Popup>

            </Grid>

                </StackPanel>

            </Grid>

            <!-- ===================================== -->
            <!-- SUMMARY CARDS -->
            <!-- ===================================== -->

            <Grid Grid.Row="1"
                  Margin="0,4,0,8">

                <Grid.ColumnDefinitions>

                    <ColumnDefinition Width="*"/>

                    <ColumnDefinition Width="14"/>

                    <ColumnDefinition Width="*"/>

                    <ColumnDefinition Width="14"/>

                    <ColumnDefinition Width="*"/>

                    <ColumnDefinition Width="14"/>

                    <ColumnDefinition Width="*"/>

                </Grid.ColumnDefinitions>


                <!-- MEMBERS -->

                <Border Grid.Column="0" Style="{StaticResource KpiCardStyle}">
                    <Grid>
                        <Border Width="4" HorizontalAlignment="Left" Background="#0A3776" CornerRadius="4,0,0,4" Margin="-20,-18,0,-18"/>
                        <StackPanel Margin="8,0,0,0">
                            <TextBlock Text="MEMBERS INSIDE"
                                       FontSize="11"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource TextMutedBrush}"/>
                            <TextBlock x:Name="MembersInsideText"
                                       Text="0"
                                       FontSize="34"
                                       FontWeight="Bold"
                                       FontFamily="Bahnschrift"
                                       Foreground="{DynamicResource AccentBrush}"
                                       Margin="0,8,0,0"/>
                            <TextBlock Text="Currently in chambers"
                                       FontSize="11"
                                       Foreground="{DynamicResource TextMutedBrush}"
                                       Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>


                <!-- CHAMBERS -->

                <Border Grid.Column="2" Style="{StaticResource KpiCardStyle}">
                    <Grid>
                        <Border Width="4" HorizontalAlignment="Left" Background="#1D6FB8" CornerRadius="4,0,0,4" Margin="-20,-18,0,-18"/>
                        <StackPanel Margin="8,0,0,0">
                            <TextBlock Text="ACTIVE CHAMBERS"
                                       FontSize="11"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource TextMutedBrush}"/>
                            <TextBlock x:Name="ActiveChambersText"
                                       Text="0"
                                       FontSize="34"
                                       FontWeight="Bold"
                                       FontFamily="Bahnschrift"
                                       Foreground="{DynamicResource AccentBrush}"
                                       Margin="0,8,0,0"/>
                            <TextBlock Text="Monitored zones"
                                       FontSize="11"
                                       Foreground="{DynamicResource TextMutedBrush}"
                                       Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>


                <!-- ALERTS -->

                <Border Grid.Column="4" Style="{StaticResource KpiCardStyle}">
                    <Grid>
                        <Border Width="4" HorizontalAlignment="Left" Background="#D97706" CornerRadius="4,0,0,4" Margin="-20,-18,0,-18"/>
                        <StackPanel Margin="8,0,0,0">
                            <TextBlock Text="ACTIVE ALERTS"
                                       FontSize="11"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource TextMutedBrush}"/>
                            <TextBlock x:Name="AlertsText"
                                       Text="0"
                                       FontSize="34"
                                       FontWeight="Bold"
                                       FontFamily="Bahnschrift"
                                       Foreground="{DynamicResource WarningBrush}"
                                       Margin="0,8,0,0"/>
                            <TextBlock Text="Attention / Warning"
                                       FontSize="11"
                                       Foreground="{DynamicResource TextMutedBrush}"
                                       Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>


                <!-- VIOLATIONS -->

                <Border Grid.Column="6" Style="{StaticResource KpiCardStyle}">
                    <Grid>
                        <Border Width="4" HorizontalAlignment="Left" Background="#DC2626" CornerRadius="4,0,0,4" Margin="-20,-18,0,-18"/>
                        <StackPanel Margin="8,0,0,0">
                            <TextBlock Text="TIME VIOLATIONS"
                                       FontSize="11"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource TextMutedBrush}"/>
                            <TextBlock x:Name="ViolationsText"
                                       Text="0"
                                       FontSize="34"
                                       FontWeight="Bold"
                                       FontFamily="Bahnschrift"
                                       Foreground="{DynamicResource DangerBrush}"
                                       Margin="0,8,0,0"/>
                            <TextBlock Text="Exceeded duration"
                                       FontSize="11"
                                       Foreground="{DynamicResource TextMutedBrush}"
                                       Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

            </Grid>

            <!-- ============================================ -->
            <!-- CHAMBER STATUS + SENSOR MONITORING -->
            <!-- ============================================ -->

            <Grid Grid.Row="2"
      Margin="0,12,0,0">

                <Grid.ColumnDefinitions>

                    <!-- Chamber Status -->
                    <ColumnDefinition Width="320"/>

                    <!-- Space -->
                    <ColumnDefinition Width="18"/>

                    <!-- Sensor Monitoring -->
                    <ColumnDefinition Width="*"/>

                </Grid.ColumnDefinitions>


                <!-- ============================================ -->
                <!-- LEFT : CHAMBER STATUS -->
                <!-- ============================================ -->

                <Grid Grid.Column="0">

                    <Grid.RowDefinitions>
                        <RowDefinition Height="38"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>


                    <!-- HEADING -->

                    <Grid Grid.Row="0">

                        <StackPanel Orientation="Horizontal"
                        VerticalAlignment="Center">

                            <TextBlock Text="Chamber Status"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimaryBrush}"/>

                            <Border Background="#ECFDF5"
                        CornerRadius="10"
                        Padding="9,4"
                        Margin="12,0,0,0">

                                <StackPanel Orientation="Horizontal">

                                    <Ellipse Width="6"
                                 Height="6"
                                 Fill="#22A06B"
                                 VerticalAlignment="Center"/>

                                    <TextBlock Text="LIVE"
                                   FontSize="9"
                                   FontWeight="Bold"
                                   Foreground="#15803D"
                                   Margin="5,0,0,0"/>

                                </StackPanel>

                            </Border>

                        </StackPanel>

                    </Grid>


                    <!-- CHAMBER CARD -->

                    <ScrollViewer Grid.Row="1"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Disabled">

                        <!-- IMPORTANT:
                 Keep only this one ChambersItemsControl
                 Do not create another one elsewhere -->

                        <ItemsControl x:Name="ChambersItemsControl">

                            <ItemsControl.ItemsPanel>

                                <ItemsPanelTemplate>

                                    <WrapPanel/>

                                </ItemsPanelTemplate>

                            </ItemsControl.ItemsPanel>


                            <ItemsControl.ItemTemplate>

                                <DataTemplate>

                                    <Border Width="300"
                                Height="155"
                                Background="White"
                                CornerRadius="16"
                                Padding="18"
                                BorderBrush="#D8E3F0"
                                BorderThickness="1"
                                Margin="0,0,12,0">

                                        <Border.Effect>

                                            <DropShadowEffect BlurRadius="16"
                                                  ShadowDepth="2"
                                                  Opacity="0.08"
                                                  Color="#0A3776"/>

                                        </Border.Effect>


                                        <Grid>

                                            <Grid.RowDefinitions>

                                                <RowDefinition Height="Auto"/>

                                                <RowDefinition Height="*"/>

                                                <RowDefinition Height="Auto"/>

                                            </Grid.RowDefinitions>


                                            <!-- TOP -->

                                            <Grid Grid.Row="0">

                                                <StackPanel>

                                                    <TextBlock Text="{Binding ChamberName}"
                                                   FontSize="16"
                                                   FontWeight="Bold"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"/>

                                                    <TextBlock Text="{Binding ChamberCode}"
                                                   FontSize="11"
                                                   Foreground="{DynamicResource TextMutedBrush}"
                                                   Margin="0,4,0,0"/>

                                                </StackPanel>


                                                <!-- OCCUPANCY -->

                                                <Border Background="{DynamicResource AvatarBgBrush}"
                                            Padding="10,6"
                                            CornerRadius="8"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Top">

                                                    <TextBlock FontSize="12"
                                                   FontWeight="Bold"
                                                   Foreground="{DynamicResource AccentBrush}">

                                            <Run Text="{Binding MemberCount}"/>

                                            <Run Text=" / "/>

                                            <Run Text="{Binding MemberThreshold}"/>

                                                    </TextBlock>

                                                </Border>

                                            </Grid>


                                            <!-- CURRENT OCCUPANCY -->

                                            <StackPanel Grid.Row="1"
                                            VerticalAlignment="Center">

                                                <TextBlock Text="CURRENT OCCUPANCY"
                                               FontSize="10"
                                               FontWeight="Bold"
                                               Foreground="{DynamicResource TextMutedBrush}"/>

                                                <TextBlock FontSize="14"
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource TextPrimaryBrush}"
                                               Margin="0,6,0,0">

                                        <Run Text="{Binding MemberCount}"/>

                                        <Run Text=" Members Inside"/>

                                                </TextBlock>

                                            </StackPanel>


                                            <!-- PROGRESS -->

                                            <ProgressBar Grid.Row="2"
                                             Height="7"
                                             Value="{Binding OccupancyPercentage, Mode=OneWay}"
                                             Maximum="100"/>

                                        </Grid>

                                    </Border>

                                </DataTemplate>

                            </ItemsControl.ItemTemplate>

                        </ItemsControl>

                    </ScrollViewer>

                </Grid>


                <!-- ============================================ -->
                <!-- RIGHT : SENSOR MONITORING -->
                <!-- ============================================ -->

                <Grid Grid.Column="2">

                    <Grid.RowDefinitions>
                        <RowDefinition Height="38"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>


                    <!-- SENSOR MONITORING HEADING -->

                    <Grid Grid.Row="0">

                        <Grid.ColumnDefinitions>

                            <ColumnDefinition Width="*"/>

                            <ColumnDefinition Width="Auto"/>

                        </Grid.ColumnDefinitions>


                        <!-- TITLE -->

                        <StackPanel VerticalAlignment="Center">

                            <TextBlock Text="Sensor Monitoring"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimaryBrush}"/>

                        </StackPanel>


                        <!-- CHAMBER + LIVE -->

                        <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        VerticalAlignment="Center">


                            <!-- CHAMBER -->

                            <Border Background="#EAF2FC"
                        CornerRadius="10"
                        Padding="10,5"
                        Margin="0,0,8,0">

                                <StackPanel Orientation="Horizontal">

                                    <Ellipse Width="6"
                                 Height="6"
                                 Fill="#0A3776"
                                 VerticalAlignment="Center"/>

                                    <TextBlock Text="Chamber 01"
                                   FontSize="10"
                                   FontWeight="SemiBold"
                                   Foreground="#0A3776"
                                   Margin="6,0,0,0"/>

                                </StackPanel>

                            </Border>


                            <!-- LIVE -->

                            <Border Background="#ECFDF5"
                        CornerRadius="10"
                        Padding="10,5">

                                <StackPanel Orientation="Horizontal">

                                    <Ellipse Width="6"
                                 Height="6"
                                 Fill="#22A06B"
                                 VerticalAlignment="Center"/>

                                    <TextBlock Text="LIVE"
                                   FontSize="10"
                                   FontWeight="Bold"
                                   Foreground="#15803D"
                                   Margin="6,0,0,0"/>

                                </StackPanel>

                            </Border>

                        </StackPanel>

                    </Grid>


                    <!-- SENSOR CARD CONTAINER -->
                        <Border Grid.Row="1"
        Background="White"
        CornerRadius="16"
        Padding="12"
        BorderBrush="#E2E8F0"
        BorderThickness="1">

                            <Border.Effect>
                                <DropShadowEffect BlurRadius="18"
                           ShadowDepth="3"
                           Opacity="0.06"/>
                            </Border.Effect>

                            <Grid>

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>


                                <!-- ================================= -->
                                <!-- TEMPERATURE -->
                                <!-- ================================= -->

                                <Border Grid.Column="0"
                                    CornerRadius="14"
                                    Padding="14"
                                    BorderBrush="#FED7AA"
                                    BorderThickness="1">        

                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#FFF7ED" Offset="0"/>
                                            <GradientStop Color="#FFEDD5" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>

                                    <Grid>

                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Text="TEMPERATURE"
                           FontSize="11"
                           FontWeight="Bold"
                           Foreground="#C2410C"/>

                                        <TextBlock x:Name="TemperatureValueText"
                           Grid.Row="1"
                           Text="--"
                           FontSize="22"
                           FontWeight="Bold"
                           FontFamily="Bahnschrift"
                           Foreground="#EA580C"
                           VerticalAlignment="Center"
                           Margin="0,4"/>

                                        <Border x:Name="TemperatureStatusBorder" 
                                            Grid.Row="2"                           
                           Background="#DCFCE7"
        BorderBrush="#4ADE80"
        BorderThickness="1"
        CornerRadius="8"
        Padding="10,5"
        HorizontalAlignment="Left">

                                            <TextBlock x:Name="TemperatureStatusText"
                                               Text="NORMAL"
                                               FontSize="12"
                                               FontWeight="Bold"
                                                        Foreground="#166534"
                                              />    

                                        </Border>

                                    </Grid>

                                </Border>


                                <!-- ================================= -->
                                <!-- HUMIDITY -->
                                <!-- ================================= -->

                                <Border Grid.Column="2"
                CornerRadius="14"
                Padding="14"
                BorderBrush="#BAE6FD"
                BorderThickness="1">

                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#F0F9FF" Offset="0"/>
                                            <GradientStop Color="#E0F2FE" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>

                                    <Grid>

                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Text="HUMIDITY"
                           FontSize="11"
                           FontWeight="Bold"
                           Foreground="#0369A1"/>

                                        <TextBlock x:Name="HumidityValueText"
                           Grid.Row="1"
                           Text="--"
                           FontSize="22"
                           FontWeight="Bold"
                           FontFamily="Bahnschrift"
                           Foreground="#0284C7"
                           VerticalAlignment="Center"
                           Margin="0,4"/>

                                        <Border x:Name="HumidityStatusBorder"  
                                            Grid.Row="2"
                       Background="#DCFCE7"
        BorderBrush="#4ADE80"
        BorderThickness="1"
        CornerRadius="8"
        Padding="10,5"
        HorizontalAlignment="Left">

                                            <TextBlock x:Name="HumidityStatusText"
           Text="NORMAL"
           FontSize="12"
           FontWeight="Bold"
                                                        Foreground="#166534"/>

                                        </Border>

                                    </Grid>

                                </Border>


                                <!-- ================================= -->
                                <!-- CO -->
                                <!-- ================================= -->

                                <Border Grid.Column="4"
                                    CornerRadius="14"
                                    Padding="14"
                                    BorderBrush="#FECACA"
                                    BorderThickness="1">    

                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#FEF2F2" Offset="0"/>
                                            <GradientStop Color="#FEE2E2" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>

                                    <Grid>

                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Text="CO"
                                           FontSize="11"
                                           FontWeight="Bold"
                                           Foreground="#B91C1C"/>

                                        <TextBlock x:Name="CoValueText"
                                           Grid.Row="1"
                                           Text="--"
                                           FontSize="22"
                                           FontWeight="Bold"
                                           FontFamily="Bahnschrift"
                                           Foreground="#DC2626"
                                           VerticalAlignment="Center"
                                           Margin="0,4"/>

                                        <Border x:Name="CoStatusBorder" 
                                            Grid.Row="2"
        Background="#DCFCE7"
        BorderBrush="#4ADE80"
        BorderThickness="1"
        CornerRadius="8"
        Padding="10,5"
        HorizontalAlignment="Left">

                                            <TextBlock x:Name="CoStatusText"
                   Text="NORMAL"
                   FontSize="12"
                   FontWeight="Bold"
                                                        Foreground="#166534"/>      

                                        </Border>

                                    </Grid>

                                </Border>


                                <!-- ================================= -->
                                <!-- CO2 -->
                                <!-- ================================= -->

                                <Border Grid.Column="6"
                                    CornerRadius="14"
                                    Padding="14"
                                    BorderBrush="#DDD6FE"
                                    BorderThickness="1">    

                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#FAF5FF" Offset="0"/>
                                            <GradientStop Color="#F3E8FF" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>

                                    <Grid>

                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Text="CO₂"
                                           FontSize="11"
                                           FontWeight="Bold"
                                           Foreground="#6D28D9"/>

                                        <TextBlock x:Name="Co2ValueText"
                                           Grid.Row="1"
                                           Text="--"
                                           FontSize="22"
                                           FontWeight="Bold"
                                           FontFamily="Bahnschrift"
                                           Foreground="#7C3AED"
                                           VerticalAlignment="Center"
                                           Margin="0,4"/>

                                        <Border x:Name="Co2StatusBorder"  
                                            Grid.Row="2"
                                            Background="#DCFCE7"
                                            BorderBrush="#4ADE80"
                                            BorderThickness="1"
                                            CornerRadius="8"
                                            Padding="10,5"
                                            HorizontalAlignment="Left">     

                                            <TextBlock x:Name="Co2StatusText"
                                           Text="NORMAL"
                                           FontSize="12"
                                           FontWeight="Bold"
                                                   Foreground="#166534"/>  

                                        </Border>

                                    </Grid>

                                </Border>


                                <!-- ================================= -->
                                <!-- OXYGEN -->
                                <!-- ================================= -->

                                <Border Grid.Column="8"
                                    CornerRadius="14"
                                    Padding="14"
                                    BorderBrush="#BBF7D0"
                                    BorderThickness="1">            

                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#F0FDF4" Offset="0"/>
                                            <GradientStop Color="#DCFCE7" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>

                                    <Grid>

                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Text="OXYGEN"
                           FontSize="11"
                           FontWeight="Bold"
                           Foreground="#166534"/>

                                        <TextBlock x:Name="OxygenValueText"
                           Grid.Row="1"
                           Text="--"
                           FontSize="22"
                           FontWeight="Bold"
                           FontFamily="Bahnschrift"
                           Foreground="#15803D"
                           VerticalAlignment="Center"
                           Margin="0,4"/>

                                        <Border x:Name="OxygenStatusBorder" 
                                            Grid.Row="2"
                                            Background="#DCFCE7"
                                            BorderBrush="#4ADE80"
                                            BorderThickness="1"
                                            CornerRadius="8"
                                            Padding="10,5"
                                            HorizontalAlignment="Left">     

                                            <TextBlock x:Name="OxygenStatusText"
                                           Text="NORMAL"
                                           FontSize="12"
                                           FontWeight="Bold"
                                                        Foreground="#166534"/>  

                                        </Border>

                                    </Grid>

                                </Border>

                            </Grid>

                        </Border>

                    </Grid>

            </Grid>



            <!-- ===================================== -->
            <!-- CURRENT MEMBERS -->
            <!-- ===================================== -->

            <Border Grid.Row="3"
				Margin="0,18,0,0"
				Background="{DynamicResource CardBgBrush}"
				CornerRadius="18"
				BorderBrush="{DynamicResource CardBorderBrush}"
				BorderThickness="1">

                <Border.Effect>
                    <DropShadowEffect BlurRadius="20"
                          ShadowDepth="2"
                          Opacity="0.09"
                          Color="#0A3776"/>
                </Border.Effect>

                <Grid>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="78"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>


                    <!-- HEADER -->

                    <Grid Grid.Row="0"
              Margin="24,0">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel VerticalAlignment="Center">

                            <TextBlock Text="Current Members"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimaryBrush}"/>

                            <TextBlock Text="Employees currently inside monitored chambers"
                           FontSize="12"
                           Foreground="{DynamicResource TextMutedBrush}"
                           Margin="0,5,0,0"/>

                        </StackPanel>


                        <StackPanel Grid.Column="1"
                                    Orientation="Horizontal"
                                    VerticalAlignment="Center">

                            <!-- LIVE BADGE -->

                            <Border VerticalAlignment="Center"
                                    Background="#ECFDF5"
                                    Padding="12,7"
                                    CornerRadius="10"
                                    Margin="0,0,10,0"
                                    BorderBrush="#BBF7D0"
                                    BorderThickness="1">

                                <StackPanel Orientation="Horizontal">

                                    <Ellipse Width="7"
                                             Height="7"
                                             Fill="#22C55E"
                                             VerticalAlignment="Center"
                                             Margin="0,0,7,0"/>

                                    <TextBlock Text="LIVE MONITORING"
                                               FontSize="10"
                                               FontWeight="Bold"
                                               Foreground="#15803D"/>

                                </StackPanel>

                            </Border>

                            <Button x:Name="StopAllVoiceButton"
                                    Content="Stop All Voice"
                                    Height="36"
                                    Padding="16,0"
                                    Margin="0,0,10,0"
                                    Background="#DC2626"
                                    Foreground="White"
                                    BorderThickness="0"
                                    FontSize="12"
                                    FontWeight="Bold"
                                    Cursor="Hand"
                                    Visibility="Collapsed"
                                    Click="StopAllVoice_Click">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="10"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>

                            <Button x:Name="ViewMoreMembersButton"
                                    Content="View More"
                                    Height="36"
                                    Padding="16,0"
                                    Background="#0A3776"
                                    Foreground="White"
                                    BorderThickness="0"
                                    FontSize="12"
                                    FontWeight="Bold"
                                    Cursor="Hand"
                                    Click="ViewMoreMembers_Click">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="10"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>

                        </StackPanel>

                    </Grid>


                    <!-- DATA GRID -->

                    <DataGrid Grid.Row="1"
                  x:Name="MembersDataGrid"
                  Margin="0,0,0,0"
                  ItemsSource="{Binding CurrentMembers}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeRows="False"
                  CanUserSortColumns="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  VerticalGridLinesBrush="Transparent"
                  BorderThickness="0"
                  Background="{DynamicResource CardBgBrush}"
                  RowHeight="62"
                  FontSize="13"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  Foreground="{DynamicResource TextPrimaryBrush}"
                  RowBackground="{DynamicResource CardBgBrush}"
                  AlternatingRowBackground="{DynamicResource AltRowBgBrush}"
                  HorizontalGridLinesBrush="{DynamicResource GridLineBrush}">

                        <!-- DATAGRID STYLE -->

                        <DataGrid.Resources>

                            <Style TargetType="DataGridRow">

                                <Setter Property="Background" Value="{DynamicResource CardBgBrush}"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Padding" Value="0"/>

                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridRow">
                                            <Border x:Name="RowBorder"
                                                    Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    SnapsToDevicePixels="True">
                                                <SelectiveScrollingGrid>
                                                    <SelectiveScrollingGrid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </SelectiveScrollingGrid.ColumnDefinitions>
                                                    <DataGridCellsPresenter Grid.Column="1"/>
                                                </SelectiveScrollingGrid>
                                            </Border>

                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="RowBorder" Property="Background" Value="{DynamicResource RowHoverBrush}"/>
                                                </Trigger>

                                                <DataTrigger Binding="{Binding Status}" Value="Attention">
                                                    <Setter TargetName="RowBorder" Property="Background" Value="{DynamicResource StatusAttentionBgBrush}"/>
                                                </DataTrigger>

                                                <DataTrigger Binding="{Binding Status}" Value="Warning">
                                                    <Setter TargetName="RowBorder" Property="Background" Value="{DynamicResource StatusWarningBgBrush}"/>
                                                </DataTrigger>

                                                <DataTrigger Binding="{Binding Status}" Value="Violation">
                                                    <Setter TargetName="RowBorder" Property="Background" Value="{DynamicResource StatusViolationBgBrush}"/>
                                                    <DataTrigger.EnterActions>
                                                        <BeginStoryboard x:Name="ViolationBlinkStoryboard">
                                                            <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                                                                <DoubleAnimation
                                                                    Storyboard.TargetName="RowBorder"
                                                                    Storyboard.TargetProperty="Opacity"
                                                                    From="1"
                                                                    To="0.55"
                                                                    Duration="0:0:0.8"/>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </DataTrigger.EnterActions>
                                                    <DataTrigger.ExitActions>
                                                        <StopStoryboard BeginStoryboardName="ViolationBlinkStoryboard"/>
                                                    </DataTrigger.ExitActions>
                                                </DataTrigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>


                            <Style TargetType="DataGridCell">

                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Padding" Value="18,0"/>
                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                                <Setter Property="Background" Value="Transparent"/>

                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                                    </Trigger>
                                </Style.Triggers>

                            </Style>

                            <Style TargetType="DataGridColumnHeader">

                                <Setter Property="Background" Value="{DynamicResource DataGridHeaderBgBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource DataGridHeaderFgBrush}"/>
                                <Setter Property="FontSize" Value="11"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Height" Value="46"/>
                                <Setter Property="Padding" Value="18,0"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource GridLineBrush}"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>

                            </Style>

                        </DataGrid.Resources>


                        <DataGrid.Columns>                           

                            <!-- EMPLOYEE -->
                            <!-- ================================= -->

                            <DataGridTemplateColumn Header="EMPLOYEE"
                            Width="2.2*">

                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>

                                        <Grid VerticalAlignment="Center">

                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="46"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Initial Circle -->

                                            <Border Grid.Column="0"
                                                Width="34"
                                                Height="34"
                                                Background="{DynamicResource AvatarBgBrush}"
                                                CornerRadius="17"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center">

                                                <TextBlock Text="👤"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   TextAlignment="Center"
                                                   FontSize="16"
                                                   Foreground="{DynamicResource AccentBrush}"/>       

                                            </Border>


                                            <!-- Name -->

                                            <StackPanel Grid.Column="1"
                                                    VerticalAlignment="Center"
                                                    Margin="8,0,0,0">

                                                <TextBlock Text="{Binding EmployeeName}"
                                                   FontSize="13"
                                                   FontWeight="SemiBold"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"/>

                                                <TextBlock Text="Currently inside"
                                                   FontSize="10"
                                                   Foreground="{DynamicResource TextMutedBrush}"
                                                   Margin="0,3,0,0"/>       

                                            </StackPanel>

                                        </Grid>

                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>

                            </DataGridTemplateColumn>


                            <!-- ================================= -->
                            <!-- ID CARD -->
                            <!-- ================================= -->

                            <DataGridTemplateColumn Header="ID CARD"
                            Width="1.1*">

                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>

                                        <TextBlock Text="{Binding EmployeeId}"
                           FontSize="12"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           VerticalAlignment="Center"/>

                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>

                            </DataGridTemplateColumn>


                            <!-- ================================= -->
                            <!-- CHAMBER -->
                            <!-- ================================= -->

                            <DataGridTemplateColumn Header="CHAMBER"
                            Width="1.4*">

                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>

                                        <Border Background="{DynamicResource ChamberChipBgBrush}"
							BorderBrush="{DynamicResource ChamberChipBorderBrush}"
							BorderThickness="1"
							CornerRadius="7"
							Padding="10,6"
							HorizontalAlignment="Left"
							VerticalAlignment="Center">

                                            <StackPanel Orientation="Horizontal">

                                                <TextBlock Text="◆"
                                   FontSize="9"
                                   Foreground="{DynamicResource AccentBrush}"
                                   Margin="0,0,7,0"
                                   VerticalAlignment="Center"/>

                                                <TextBlock Text="{Binding ChamberName}"
                                   FontSize="12"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   VerticalAlignment="Center"/>

                                            </StackPanel>

                                        </Border>

                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>

                            </DataGridTemplateColumn>



                            <!-- ================================= -->
                            <!-- IN TIME -->
                            <!-- ================================= -->

                            <DataGridTemplateColumn Header="IN TIME"
                            Width="1.5*">

                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>

                                        <StackPanel VerticalAlignment="Center">

                                            <TextBlock Text="{Binding EntryTime,
                                      StringFormat={}{0:hh:mm:ss tt}}"
                               FontSize="13"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimaryBrush}"/>

                                            <TextBlock Text="{Binding EntryTime,
                                      StringFormat={}{0:dd MMM yyyy}}"
                               FontSize="10"
                               Foreground="{DynamicResource TextMutedBrush}"
                               Margin="0,3,0,0"/>

                                        </StackPanel>

                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>

                            </DataGridTemplateColumn>
                           

                            <!-- COUNTDOWN -->

                            <DataGridTemplateColumn Header="TIME REMAINING"
                            Width="1.7*">

                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>

                                        <Border HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Padding="12,7"
                        CornerRadius="7">

                                            <Border.Style>

                                                <Style TargetType="Border">

                                                    <Setter Property="Background"
                                    Value="{DynamicResource CountdownInsideBgBrush}"/>

                                                    <Setter Property="BorderBrush"
                                    Value="{DynamicResource CountdownInsideBorderBrush}"/>

                                                    <Setter Property="BorderThickness"
                                    Value="1"/>

                                                    <Style.Triggers>

                                                        <DataTrigger Binding="{Binding Status}"
                                             Value="Attention">

                                                            <Setter Property="Background"
                                            Value="{DynamicResource CountdownAttentionBgBrush}"/>

                                                            <Setter Property="BorderBrush"
                                            Value="{DynamicResource CountdownAttentionBorderBrush}"/>

                                                        </DataTrigger>

                                                        <DataTrigger Binding="{Binding Status}"
                                             Value="Warning">

                                                            <Setter Property="Background"
                                            Value="{DynamicResource CountdownWarningBgBrush}"/>

                                                            <Setter Property="BorderBrush"
                                            Value="{DynamicResource CountdownWarningBorderBrush}"/>

                                                        </DataTrigger>

                                                        <DataTrigger Binding="{Binding Status}"
                                             Value="Alert">

                                                            <Setter Property="Background"
                                            Value="{DynamicResource CountdownWarningBgBrush}"/>

                                                            <Setter Property="BorderBrush"
                                            Value="{DynamicResource CountdownWarningBorderBrush}"/>

                                                        </DataTrigger>

                                                        <DataTrigger Binding="{Binding Status}"
             Value="Violation">

                                                            <Setter Property="Background"
            Value="{DynamicResource CountdownViolationBgBrush}"/>

                                                            <Setter Property="BorderBrush"
            Value="{DynamicResource CountdownViolationBorderBrush}"/>

                                                        </DataTrigger>

                                                    </Style.Triggers>

                                                </Style>

                                            </Border.Style>


                                            <StackPanel Orientation="Horizontal">

                                                <TextBlock Text="◷"
                                   FontSize="14"
                                   Margin="0,0,8,0"
                                   VerticalAlignment="Center"
                                   Foreground="{DynamicResource AccentBrush}"/>

                                                <TextBlock Text="{Binding Countdown}"
                                   FontSize="14"
                                   FontWeight="ExtraBold"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   VerticalAlignment="Center"/>

                                            </StackPanel>

                                        </Border>

                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>

                            </DataGridTemplateColumn>

                            <!-- ================================= -->
                            <!-- STATUS -->
                            <!-- ================================= -->

                            <DataGridTemplateColumn Header="STATUS"
                            Width="1.2*">

                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>

                                        <Border HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Padding="12,6"
                        CornerRadius="14">

                                            <Border.Style>

                                                <Style TargetType="Border">

                                                    <Setter Property="Background"
                                    Value="{DynamicResource StatusInsideBgBrush}"/>

                                                    <Style.Triggers>

                                                        <DataTrigger Binding="{Binding Status}"
                                             Value="Attention">

                                                            <Setter Property="Background"
                                            Value="{DynamicResource StatusAttentionBgBrush}"/>

                                                        </DataTrigger>

                                                        <DataTrigger Binding="{Binding Status}"
                                             Value="Warning">

                                                            <Setter Property="Background"
                                            Value="{DynamicResource StatusWarningBgBrush}"/>

                                                        </DataTrigger>

                                                        <DataTrigger Binding="{Binding Status}"
                                             Value="Alert">

                                                            <Setter Property="Background"
                                            Value="{DynamicResource StatusWarningBgBrush}"/>

                                                        </DataTrigger>

                                                        <DataTrigger Binding="{Binding Status}"
                                             Value="Violation">

                                                            <Setter Property="Background"
                                            Value="{DynamicResource CountdownViolationBgBrush}"/>

                                                        </DataTrigger>

                                                    </Style.Triggers>

                                                </Style>

                                            </Border.Style>


                                            <StackPanel Orientation="Horizontal">

                                                <Ellipse Width="7"
                                 Height="7"
                                 VerticalAlignment="Center"
                                 Margin="0,0,7,0">

                                                    <Ellipse.Style>

                                                        <Style TargetType="Ellipse">

                                                            <Setter Property="Fill"
                                            Value="#22C55E"/>

                                                            <Style.Triggers>

                                                                <DataTrigger Binding="{Binding Status}"
                                                     Value="Attention">

                                                                    <Setter Property="Fill"
                                                    Value="#38BDF8"/>

                                                                </DataTrigger>

                                                                <DataTrigger Binding="{Binding Status}"
                                                     Value="Warning">

                                                                    <Setter Property="Fill"
                                                    Value="#FBBF24"/>

                                                                </DataTrigger>

                                                                <DataTrigger Binding="{Binding Status}"
                                                     Value="Alert">

                                                                    <Setter Property="Fill"
                                                    Value="#FBBF24"/>

                                                                </DataTrigger>

                                                                <DataTrigger Binding="{Binding Status}"
                                                     Value="Violation">

                                                                    <Setter Property="Fill"
                                                    Value="#F87171"/>

                                                                </DataTrigger>

                                                            </Style.Triggers>

                                                        </Style>

                                                    </Ellipse.Style>

                                                </Ellipse>


                                                <TextBlock Text="{Binding Status}"
                                   FontSize="11"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   VerticalAlignment="Center"/>

                                            </StackPanel>

                                        </Border>

                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>

                            </DataGridTemplateColumn>

                            <!-- ================================= -->
                            <!-- STOP VOICE -->
                            <!-- ================================= -->

                            <DataGridTemplateColumn Header="VOICE"
                            Width="0.9*">

                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>

                                        <Button Content="Stop"
                                                Width="72"
                                                Height="30"
                                                FontSize="12"
                                                FontWeight="Bold"
                                                Cursor="Hand"
                                                Background="#DC2626"
                                                Foreground="White"
                                                BorderThickness="0"
                                                Click="StopMemberVoice_Click"
                                                Visibility="{Binding ShowStopVoice, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                            CornerRadius="8"
                                                            Padding="8,0">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>

                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>

                            </DataGridTemplateColumn>

                        </DataGrid.Columns>

                    </DataGrid>

                </Grid>

            </Border>

            </Grid>

        </Grid>


        <!-- DARK MODE BOOT OVERLAY -->
        <Border x:Name="SciFiBootOverlay"
                Grid.ColumnSpan="2"
                Visibility="Collapsed"
                Panel.ZIndex="9999"
                Background="#E0020612">

            <Grid>
                <Border x:Name="SciFiScanLine"
                        Height="3"
                        VerticalAlignment="Top"
                        Background="#AA22D3EE"
                        Opacity="0.85"/>

                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="SRP // SYSTEMS"
                               FontSize="12"
                               FontWeight="Bold"
                               Foreground="#67E8F9"
                               HorizontalAlignment="Center"/>
                    <TextBlock x:Name="SciFiBootTitle"
                               Text="DARK MODE ENGAGED"
                               FontSize="34"
                               FontWeight="Bold"
                               Foreground="#ECFEFF"
                               HorizontalAlignment="Center"
                               Margin="0,12,0,0"/>
                    <TextBlock x:Name="SciFiBootSubtitle"
                               Text="Switching to dark interface..."
                               FontSize="14"
                               Foreground="#22D3EE"
                               HorizontalAlignment="Center"
                               Margin="0,10,0,0"/>
                    <Border Width="280" Height="4" CornerRadius="2" Background="#164E63" Margin="0,24,0,0">
                        <Border x:Name="SciFiBootProgress"
                                Width="0"
                                Height="4"
                                CornerRadius="2"
                                Background="#22D3EE"
                                HorizontalAlignment="Left"/>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

    </Grid>

</Page>